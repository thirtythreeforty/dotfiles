[user]
	email = thirtythreeforty@gmail.com
	name = George Hilliard
[alias]
	unstage = reset HEAD --
	visual = !gitk --all
	ours   = "!f() { git checkout --ours $@ && git add $@; }; f"
	theirs = "!f() { git checkout --theirs $@ && git add $@; }; f"
	st = status
	co = checkout
	ci = commit
	ds = diff --staged
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(bold yellow)%d%Creset %s %Cgreen(%cr) %C(blue)<%an>%Creset' --abbrev-commit --all
	amend = commit --amend -C HEAD
	ignore = "!f() { curl http://www.gitignore.io/api/$@ ;}; f >> .gitignore"
	undo = reset --merge master@{1}
[diff]
	algorithm = patience
	tool = xxdiff
[color]
	ui = true
[gui]
	historybrowser = gitk
[push]
	default = simple
[fetch]
	prune = true
[core]
	excludesfile = /home/georgev/.gitignore_global
	pager = less -x1,5
[rerere]
	enabled = true
[init]
	templatedir = ~/.git_template
[include]
	path = .gitconfig_local
